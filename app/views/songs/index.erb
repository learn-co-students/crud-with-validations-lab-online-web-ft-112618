<table>
  <thead>
    <tr>
        <th>Song Title</th>
    </tr>
  </thead>
  <tbody>
    <% @songs.each do |song| %>
      <tr><td><%= link_to song.title, song_path(song) %> | <%= link_to "Edit Song", edit_song_path(song) %></td></tr>
    <% end %>
  </tbody>
</table>